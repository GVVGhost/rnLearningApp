export enum Indent {
  XS = 2,
  S = 4,
  M = 6,
  L = 8,
  XL = 16,
}

export enum CornerRadius {
  XS = 3,
  S = 6,
  M = 10,
  L = 14,
  XL = 20,
}

export enum IconSize {
  XS = 10,
  S = 20,
  M = 30,
  L = 40,
  XL = 50,
}

export enum BorderWidth {
  S = 0.5,
  M = 1,
  L = 2,
}

export enum Elevation {
  S = 3,
  M = 5,
  L = 8,
}

export enum FontSize {
  XS = 10,
  S = 14,
  M = 18,
  L = 24,
  XL = 32,
}

export enum ViewSize {
  XS = 10,
  S = 20,
  M = 30,
  L = 40,
  XL = 50,
}

export enum LargeViewSize {
  S = 75,
  M = 100,
  L = 125,
}
